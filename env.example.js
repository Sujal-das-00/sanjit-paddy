window.APP_CONFIG = window.APP_CONFIG || {};

// Use "/api" when the frontend is deployed behind the same domain/rewrite.
// Use the full hosted URL when running the frontend locally against the hosted backend.
window.APP_CONFIG.API_BASE_URL = "https://api.toolszila.com/api";
